/** Leaderboard — Step 9 will load GET /api/ranking. */
export default function RankingPage() {
  return (
    <div className="page ranking-page">
      <h1>Ranking</h1>
      <p className="page-message">
        Best-score table will be built in Step 9.
      </p>
    </div>
  );
}
