/** Game shell — setup → planning → execution → result (Step 8). */
export default function GamePage() {
  return (
    <div className="page game-page">
      <h1>Play</h1>
      <p className="page-message">
        Game UI (setup, planning, execution, result) will be built in Step 8.
      </p>
    </div>
  );
}
